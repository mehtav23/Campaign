language: node_js
node_js:
  - "8.11"
before_install:
- npm install -g @angular/cli
install: npm install
branches:
  only:
  - master
deploy:
  provider: heroku
  api_key:
    secure: $heroku_key
  app: campaign-organizer